build/
build-metal/
.DS_Store
compile_commands.json

# Editor state -- clangd's index, machine-local.
.cache/

# Downloaded weights: 2.4 GB of GGUF plus the SNAC codec.
models/

# Synthesised audio. `--out` defaults to out.wav in the working directory.
*.wav
fox*.png
